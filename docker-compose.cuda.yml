services:
  api:
    environment:
      VBRIDGE_INFERENCE_BACKEND: cuda
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]

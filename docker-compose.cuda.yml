services:
  api:
    image: vbridge-api:inference-cuda
    build:
      context: .
      dockerfile: docker/inference.cuda.Dockerfile
    environment:
      VBRIDGE_INFERENCE_BACKEND: cuda
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
